/**
 * Backend Constants - Only what's actually used by the server
 */

// Component Priority Order (used in contentful/client.ts for sorting)
export const COMPONENT_PRIORITY_ORDER = ['hero', 'seoText', 'faqs'] as const;
